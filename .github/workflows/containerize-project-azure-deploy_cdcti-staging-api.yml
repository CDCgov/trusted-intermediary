# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - cdcti-staging-api

on:
  push:
    branches:
      - containerize-project-azure-deploy
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to registry
      uses: docker/login-action@v1
      with:
        registry: https://cdctistagingcontainerregistry.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_0d15fb060b85458aa5adf93a065ec4b1 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_12b81cc870c04a1581fd17f968b4cf4a }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: cdctistagingcontainerregistry.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_0d15fb060b85458aa5adf93a065ec4b1 }}/cdctistagingcontainerregistry/trusted-intermediary-web:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'cdcti-staging-api'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_13e7bd655b724d3da719ca003727ff55 }}
        images: 'cdctistagingcontainerregistry.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_0d15fb060b85458aa5adf93a065ec4b1 }}/cdctistagingcontainerregistry/trusted-intermediary-web:${{ github.sha }}'